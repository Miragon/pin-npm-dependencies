version: 2
updates:
  # Keep GitHub Actions in workflows up to date (actions/checkout, actions/setup-node, etc.)
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
